*** Begin Patch
*** Update File: src/data/catalog.js
@@
-function updateItem(code, patch) {
-  const items = readCatalog();
-  const idx = items.findIndex(i => i.code.toUpperCase() === String(code).toUpperCase());
-  if (idx === -1) return { ok: false, error: 'Item not found' };
-  const merged = { ...items[idx], ...patch, code: items[idx].code }; // code is immutable
-  const v = validateItem(merged);
-  if (!v.ok) return { ok: false, error: v.errors.join('; ') };
-  items[idx] = v.item;
-  writeCatalog(items);
-  return { ok: true, item: v.item };
-}
+function isMeaningfullyEmpty(value) {
+  if (value === undefined || value === null) return true;
+  if (typeof value === 'string') return value.trim() === '';
+  if (Array.isArray(value)) return value.length === 0;
+  if (typeof value === 'object') return Object.keys(value).length === 0;
+  return false;
+}
+
+/**
+ * Safe partial update.
+ *
+ * Important guarantees:
+ * - Editing one field does not erase other saved product fields.
+ * - Empty placeholder values from a future/reworked admin UI do not erase
+ *   existing values.
+ * - Unknown/future fields on an existing product are preserved.
+ * - A field can still be intentionally cleared by passing its name in
+ *   patch.__clearFields.
+ * - Product code remains immutable.
+ */
+function updateItem(code, patch = {}) {
+  const items = readCatalog();
+  const idx = items.findIndex(i => i.code.toUpperCase() === String(code).toUpperCase());
+  if (idx === -1) return { ok: false, error: 'Item not found' };
+
+  const existing = items[idx];
+  const incoming = (patch && typeof patch === 'object') ? { ...patch } : {};
+
+  const clearFields = new Set(
+    Array.isArray(incoming.__clearFields)
+      ? incoming.__clearFields.map(String)
+      : []
+  );
+  delete incoming.__clearFields;
+
+  // Fields commonly sent by an edit form. Preserve the stored value when
+  // the new UI sends an empty placeholder for a field the user did not edit.
+  const protectedFields = [
+    'description',
+    'images',
+    'img',
+    'weightOptions',
+    'nutritionInfo',
+    'storageTips',
+    'cutTypes',
+    'boneOptions',
+    'freshnessGuarantee',
+    'returnPolicy'
+  ];
+
+  for (const field of protectedFields) {
+    if (!Object.prototype.hasOwnProperty.call(incoming, field)) continue;
+    if (clearFields.has(field)) continue;
+
+    if (
+      isMeaningfullyEmpty(incoming[field]) &&
+      !isMeaningfullyEmpty(existing[field])
+    ) {
+      delete incoming[field];
+    }
+  }
+
+  // Do not let a blank required field from a redesigned form erase a valid
+  // existing value.
+  for (const field of ['name', 'unit', 'cat']) {
+    if (
+      Object.prototype.hasOwnProperty.call(incoming, field) &&
+      typeof incoming[field] === 'string' &&
+      incoming[field].trim() === '' &&
+      existing[field] !== undefined
+    ) {
+      delete incoming[field];
+    }
+  }
+
+  // Likewise, an empty price input means "not edited", not zero/invalid.
+  if (
+    Object.prototype.hasOwnProperty.call(incoming, 'price') &&
+    (incoming.price === '' || incoming.price === null || incoming.price === undefined)
+  ) {
+    delete incoming.price;
+  }
+
+  const merged = { ...existing, ...incoming, code: existing.code };
+  const v = validateItem(merged);
+  if (!v.ok) return { ok: false, error: v.errors.join('; ') };
+
+  // Start with the complete existing record so fields unknown to the current
+  // code (including future feature fields) are never silently dropped.
+  const updated = { ...existing };
+
+  // Apply normalized fields currently understood by validateItem().
+  for (const [key, value] of Object.entries(v.item)) {
+    updated[key] = value;
+  }
+
+  // Preserve/apply future fields sent by a newer UI.
+  for (const [key, value] of Object.entries(incoming)) {
+    if (key !== 'code') updated[key] = value;
+  }
+
+  updated.code = existing.code;
+
+  items[idx] = updated;
+  writeCatalog(items);
+  return { ok: true, item: updated };
+}
*** End Patch
